# Dependencies (reinstalled inside the image)
node_modules
**/node_modules

# Runtime state — never bake an index or someone's notes into the image
data
docs
.ragdown

# VCS, tests, local config
.git
coverage
web/dist
site
.env
.env.local
*.log
.claude

# Docker files themselves
Dockerfile
docker-compose.yml
.dockerignore
